// Define a function mul(a, b) that takes two non-negative integers a
// and b and returns their product without "*".

function mul(a, b) {
  var product = 0;
  for (i = 0; i < b; i ++){
    product += a
  }
  return product;
}
